import { Tabs } from "./tab";

export const Works = () => {

    return(
        <div id="Works" className="w-full h-[calc(100vh-200px)]  bg-amber-300 relative z-1" style={{cursor: "pointer"}}>
            <Tabs/>
        </div>
    );
}